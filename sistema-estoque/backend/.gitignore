node_modules
.env

# Versiona o exemplo de ambiente e o lockfile (sobrescreve regras herdadas)
!.env.example
!package-lock.json
# Banco SQLite gerado localmente (recriado via migrate + seed)
prisma/*.db
prisma/*.db-journal
.DS_Store
npm-debug.log*
