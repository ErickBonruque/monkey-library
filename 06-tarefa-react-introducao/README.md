# 06 - Tarefa React: Introdução

Projeto simples em React + TypeScript com:

- Componentes reutilizáveis (`Header`, `CardPerfil`, `ListaHabilidades`)
- Navegação com 3 páginas (`/`, `/perfil`, `/habilidades`)

## Como executar

```bash
npm install
npm run dev
```
